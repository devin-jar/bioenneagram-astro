---
import Layout from '~/layouts/Layout.astro';
import { getHomePermalink } from '~/utils/permalinks';

const title = `Construyendo`;
---

<Layout metadata={{ title }}>
  <section class="flex items-center h-full p-16">
    <div class="container flex flex-col items-center justify-center px-5 mx-auto my-8">
      <div class="max-w-md text-center">
        <h2 class="mb-8 font-bold text-4xl sm:text-5xl md:text-6xl">
          <span class="sr-only">En Desarrollo</span>
          <span class="text-primary">Construyendo</span>
          <span>👨‍💻</span>
        </h2>
        <p class="text-3xl font-semibold md:text-3xl">Lo sentimos, esta página está en construcción.</p>
        <p class="mt-4 mb-8 text-lg text-muted dark:text-slate-400">
          Pero no te preocupes, puedes encontrar muchas otras cosas en nuestra página principal.
        </p>
        <a rel="noopener noreferrer" href={getHomePermalink()} class="btn ml-4">Regresar al Inicio</a>
      </div>
    </div>
  </section>
</Layout>
